> When same user logIN after some time it should clear the DOM nodes inserted before
> Exit Button implement.
> After sb leaves the other should go back to landingPage without loading status with a message
